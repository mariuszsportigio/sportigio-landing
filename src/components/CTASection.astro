<section class="relative py-12 overflow-hidden">
  <!-- Gradient Background -->
  <div class="absolute inset-0 sportigio-gradient-5"></div>
  
  <!-- Subtle Color Overlays -->
  <div class="absolute inset-0">
    <div class="absolute inset-0 bg-gradient-to-tr from-transparent via-accent/10 to-transparent"></div>
    <div class="absolute inset-0 bg-gradient-to-bl from-secondary/8 via-transparent to-accent/6"></div>
  </div>
  
  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-15">
    <div class="absolute top-8 left-8 w-24 h-24 bg-secondary/20 rounded-full blur-xl"></div>
    <div class="absolute top-24 right-16 w-20 h-20 bg-accent/15 rounded-full blur-lg"></div>
    <div class="absolute bottom-16 left-1/4 w-28 h-28 bg-white/10 rounded-full blur-2xl"></div>
    <div class="absolute bottom-24 right-8 w-22 h-22 bg-secondary/12 rounded-full blur-xl"></div>
    <div class="absolute top-1/2 left-1/3 w-26 h-26 bg-accent/8 rounded-full blur-2xl"></div>
  </div>

  <div class="relative z-10 max-w-4xl mx-auto px-4 text-center">
    <!-- Badge -->
    <div class="inline-flex items-center px-4 py-2 bg-white/15 backdrop-blur-sm border border-white/20 rounded-full text-sm font-medium text-white mb-6">
      <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
      </svg>
      Rozpocznij już dziś
    </div>

    <!-- Main Heading -->
    <h2 class="text-3xl lg:text-5xl font-extrabold text-white mb-6 leading-tight font-poppins">
      Dołącz do nowej ery
      <span class="block text-transparent bg-clip-text bg-gradient-to-r from-secondary via-cyan-200 to-white">
        klubów sportowych
      </span>
    </h2>

    <!-- Subheading -->
    <p class="text-base lg:text-lg text-gray-200 mb-6 max-w-3xl mx-auto leading-relaxed">
      Ponad 500 klubów już korzysta z Sportigio. Dołącz do grona liderów, 
      którzy wybrali nowoczesność, efektywność i wygodę.
    </p>

    <!-- Stats Row -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
      <div class="text-center">
        <div class="text-3xl lg:text-4xl font-extrabold text-white mb-2">500+</div>
        <div class="text-gray-300 text-sm">Aktywnych klubów</div>
      </div>
      <div class="text-center">
        <div class="text-3xl lg:text-4xl font-extrabold text-white mb-2">50k+</div>
        <div class="text-gray-300 text-sm">Zadowolonych użytkowników</div>
      </div>
      <div class="text-center">
        <div class="text-3xl lg:text-4xl font-extrabold text-white mb-2">98%</div>
        <div class="text-gray-300 text-sm">Wskaźnik zadowolenia</div>
      </div>
    </div>

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center mb-6">
      <button class="bg-secondary text-primary group px-6 py-3 font-semibold text-sm rounded-xl transition-all duration-300 transform hover:scale-105 shadow-xl">
        <span class="flex items-center">
          Rozpocznij za darmo
          <svg class="w-4 h-4 ml-2 group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
        </span>
      </button>
      
      <button class="px-6 py-3 border-2 border-white/25 text-white font-semibold text-sm rounded-xl hover:bg-white/10 backdrop-blur-sm transition-all duration-300 transform hover:scale-105">
        Zobacz demo na żywo
      </button>
    </div>

    <!-- Trust Elements -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4 text-gray-300 text-sm">
      <div class="flex items-center">
        <svg class="w-4 h-4 mr-2 text-secondary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
        30 dni za darmo
      </div>
      <div class="flex items-center">
        <svg class="w-4 h-4 mr-2 text-secondary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
        Anuluj w każdym momencie
      </div>
      <div class="flex items-center">
        <svg class="w-4 h-4 mr-2 text-secondary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
        Setup w 5 minut
      </div>
    </div>
  </div>
</section>

<!-- Floating Elements Animation -->
<script>
  document.addEventListener('DOMContentLoaded', () => {
    // Add floating animation to background elements
    const floatingElements = document.querySelectorAll<HTMLElement>('.absolute [class*="blur"]');
    
    floatingElements.forEach((element, index) => {
      const animationDuration = 3 + (index * 0.5); // Different speeds
      const animationDelay = index * 0.2; // Staggered start
      
      element.style.animation = `float ${animationDuration}s ease-in-out infinite`;
      element.style.animationDelay = `${animationDelay}s`;
    });
  });
</script>

<style>
  @keyframes float {
    0%, 100% {
      transform: translateY(0px) scale(1);
    }
    50% {
      transform: translateY(-15px) scale(1.03);
    }
  }
</style> 
